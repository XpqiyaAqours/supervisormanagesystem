<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smsys.system.mapper.StudentMapper">
    <select id="getStudentDetail" resultType="com.smsys.system.model.StudentDetail">
        SELECT
            a.id,
            student_no AS studentNo,
            student_name AS studentName,
            case sex when '0' then '男'
                     when '1' then '女'
                end as sex,
            belong_college AS belongCollege
        FROM
            sys_student a INNER JOIN sys_user b ON a.user_id = b.id
        WHERE
            b.id = #{id}

    </select>


</mapper>
